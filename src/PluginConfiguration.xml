<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <plugin displayName="Test Plugin" />

  <modes>
    <mode name="Main" />
  </modes>

  <actions>
    <group displayName="Edit">
      <command name="Undo" description="Undo the last action" />
    </group>
    <group displayName="Adjustments">
      <adjustment name="Contrast" description="Modify image contrast" />
    </group>
  </actions>

  <layouts>
    <layout name="Main">
      <!-- Edit -->
      <command name="Undo" defaultControls="Undo" />
      <!-- Adjustments -->
      <adjustment name="Contrast" defaultControls="Contrast" />
    </layout>
  </layouts>

</configuration>
